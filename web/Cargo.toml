[package]
edition = "2024"
name = "regex-strings-demo"
publish = false
version = "0.1.0"

[workspace]

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
js-sys = "0.3"
regex = "1.13.1"
regex-strings = { path = "../regex-strings" }
wasm-bindgen = "0.2"

[package.metadata.wasm-pack.profile.release]
wasm-opt = false
